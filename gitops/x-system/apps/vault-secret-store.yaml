project: default
source:
  # ВАЖНО: замени на тот же URL, что и gitops_repo_url в terraform.tfvars —
  # это не helm-чарт, а обычная папка с YAML в твоём же репозитории.
  repoURL: https://github.com/bohdan-ihnatenko/laba.git
  targetRevision: main
  path: gitops/manifests
destination:
  namespace: external-secrets

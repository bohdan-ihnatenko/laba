apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kibana
  namespace: logging
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  hostnames:
    - "kibana.hydranoid.site"
  rules:
    - backendRefs:
        - name: kibana-kb-http
          port: 5601

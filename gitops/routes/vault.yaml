apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vault
  namespace: vault
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  hostnames:
    - "vault.hydranoid.site"
  rules:
    - backendRefs:
        - name: vault
          port: 8200
